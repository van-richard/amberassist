# Notes

Template files for QM/MM simulations with semi-empirical/DFT methods
- Copy the directory `input` to your project
- Modify `make_info.sh`

## SLURM scripts
1. `runqmmm1.slurm` - running initial pull and reverse pull (step5)
2. `run.slurm` - run independent simulations (step6)
