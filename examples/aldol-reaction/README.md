# Aldol-Reaction CV Example

This directory preserves an experimental, project-specific collective-variable
generator for an aldol-reaction workflow.

The script contains system-specific atom masks, restraint endpoints, window
counts, filenames, and paths. It is retained as a research example and should
not be used as a general AMBER default.
